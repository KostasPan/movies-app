import { SearchTabValidationDirective } from './search-tab-validation.directive';

describe('SearchTabValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchTabValidationDirective();
    expect(directive).toBeTruthy();
  });
});
